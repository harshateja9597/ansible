- name: loops
  hosts: localhost
  tasks:
  - name: print persons
    ansible.builtin.debug:
      msg: "hello{{item}}" #item is an default keyword
    loop:
    - harsha
    - teja
    - aadhya
    - bhargavi
